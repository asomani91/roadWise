import React, {
    useState,
    useEffect
} from 'react'
import API from "../../utils/API"
const Cars = ({ Cars }) => {
    // const Car = [car, setCar] = useState({})

    return (
        <> {
            Cars
        } cars
        </>)
}

export default Cars